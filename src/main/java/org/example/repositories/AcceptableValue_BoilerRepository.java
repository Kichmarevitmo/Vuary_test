package org.example.repositories;

import org.example.entities.AcceptableValue_Boiler;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AcceptableValue_BoilerRepository extends JpaRepository<AcceptableValue_Boiler, Long> {
}
