package org.example.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

public interface Characteristic_SeriesRepository extends JpaRepository<Characteristic_SeriesRepository, Long> {
}
